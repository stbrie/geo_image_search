# Runtime dependencies for geo_image_search (CLI + GUI).
# The GUI itself uses only the Python stdlib (tkinter).
#
# Install with:
#   python -m venv .venv
#   .venv\Scripts\python -m pip install -r requirements.txt   (Windows)
#   .venv/bin/python -m pip install -r requirements.txt       (macOS/Linux)

geopy==2.4.1
exif==1.6.1

# KML export
fastkml==1.4.0
pygeoif==1.6.0
shapely==2.1.2
# lxml is optional for fastkml but required for pretty-printed KML output.
lxml==6.1.0
