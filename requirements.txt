# Runtime dependencies for geo_image_search (CLI + GUI).
# The GUI itself uses only the Python stdlib (tkinter).
#
# Install with:
#   python -m venv .venv
#   .venv\Scripts\python -m pip install -r requirements.txt   (Windows)
#   .venv/bin/python -m pip install -r requirements.txt       (macOS/Linux)

geopy==2.4.1
exif==1.6.1

# HEIC support — registers .heic/.heif as PIL openers. Pulls in pillow.
pillow-heif==1.3.0

# KML export
fastkml==1.4.0
pygeoif==1.6.0
shapely==2.1.2
# lxml is optional for fastkml but required for pretty-printed KML output.
lxml==6.1.0

# Build-time only: used by build.ps1 to compile geo_image_search.exe.
# Not needed at runtime. Requires a Microsoft C/C++ compiler (Visual
# Studio Build Tools or full VS) — Nuitka does NOT install one.
nuitka==4.1
